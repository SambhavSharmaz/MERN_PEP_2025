// let age = 25;
// const year =2025;
// var block = 22;
// console.log(`age is ${age} and year is ${year} with block ${block}`);

// console.log(name);
// var name = 10;
// console.log(name);
// var name = 10;
// console.log(name);

// JavaScript has 8 Datatypes: String, Number, Bigint, Boolean, Undefined, Null, Symbol, Object.
//memoral allocation is a process of allocating memory for the variables and objects. It is done at runtime.

// let name = 10;
// console.log(name);

// cant have the same name for var and let in the same scope

console.log(name);
let name = 10;
console.log(name);

//Uncaught ReferenceError: Cannot access 'name' before initialization beacuse let is block scoped, why?: it is not allowed to use let before declaration

//temporal dead zone is a period of time during which a variable is declared but not yet initialized. such as let and const

//variable have the default value of undefined
//access before declaration is not allowed for let and const
//re-declaration is not allowed for let and const
//re-initialization is allowed for let and const

// + can be used for string concatenation and number addition, - can be used for number subtraction and string comparison, * can be used for multiplication and / can be used for division.
// 1 + 1 -> 2, 1 + '1' -> '11', '1' + '1' ->, '11', 'a' + 1 -> 'a1'
// 1 - 1 -> 0, 1 - '1' -> 0 , '1' - '1' -> 0, 'a' - 1 -> NaN
// 1 * 1 -> 1, 1 * '1' -> 1 , '1' * '1' -> 1, 'a' * 1 -> NaN
// 1 / 1 -> 1, 1 / '1' -> 1 , '1' / '1' -> 1, 'a' / 1 -> NaN 
 
// Steps to push repo in github: 1) git add . 2) git commit -m "____" 3) git push origin main
